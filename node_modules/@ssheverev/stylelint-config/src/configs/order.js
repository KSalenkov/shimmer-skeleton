const propertyGroups = require('stylelint-config-recess-order/groups');

module.exports = {
  extends: ['../reset/order.js'],
  rules: {
    'order/order': [
      'custom-properties',
      'dollar-variables',
      {
        type: 'at-rule',
        name: 'extend',
      },
      {
        type: 'at-rule',
        name: 'include',
      },
      'declarations',
      'rules',
      {
        type: 'at-rule',
        name: 'keyframes',
      },
      {
        type: 'at-rule',
        name: 'media',
      },
    ],
    'order/properties-order': propertyGroups.map((group) => ({
      ...group,
      emptyLineBefore: 'always',
      noEmptyLineBetween: true,
    })),
  }
};
