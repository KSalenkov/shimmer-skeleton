import resetPromise from '../reset/promise.js';

export default [
  resetPromise,
  {
    rules: {
      'promise/no-return-in-finally': 'error',
      'promise/param-names': [
        'error',
        {
          resolvePattern: 'resolve',
          rejectPattern: 'reject'
        }
      ],
      'promise/prefer-catch': 'error',
    },
  },
];
