import resetPrettier from '../reset/prettier.js';
import eslintConfigPrettier from 'eslint-config-prettier';

export default [
  resetPrettier,
  eslintConfigPrettier,
  {
    rules: {
      'prettier/prettier': 'warn',
    },
  },
];
