import promise from 'eslint-plugin-promise'

export default {
  /*
   * Сброс всех правил в состояние "Нет ошибки". Для плавной интеграции
   */
  plugins: {
    promise
  },
  rules: {
    // Description: Require returning inside each then() to create readable and reusable Promise chains.
    // Description RU: Требует возврата внутри каждой функции then() для создания читаемых и повторно используемых цепочек обещаний.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/always-return.md
    'promise/always-return': 'off',

    // Description: Disallow creating new promises outside of utility libs (use util.promisify instead).
    // Description RU: Запрещает создание новых обещаний за пределами служебных библиотек (вместо этого используйте util.promisify).
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/avoid-new.md
    'promise/avoid-new': 'off',

    // Description: Enforce the use of catch() on un-returned promises.
    // Description RU: Принудительное использование функции catch() для невыполненных обещаний.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/catch-or-return.md
    'promise/catch-or-return': 'off',

    // Description: Disallow calling cb() inside of a then() (use util.callbackify instead).
    // Description RU: Запретить вызов cb() внутри then() (вместо этого используйте util.callbackify).
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/no-callback-in-promise.md
    'promise/no-callback-in-promise': 'off',

    // Description: Disallow creating new promises with paths that resolve multiple times.
    // Description RU: Запрещает создание новых обещаний с путями, которые разрешаются несколько раз.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/no-multiple-resolved.md
    'promise/no-multiple-resolved': 'off',

    // Description: Require creating a Promise constructor before using it in an ES5 environment.
    // Description RU: Требует создания конструктора Promise перед его использованием в среде ES5.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/no-native.md
    'promise/no-native': 'off',

    // Description: Disallow nested then() or catch() statements.
    // Description RU: Запрещает использование вложенных инструкций then() или catch().
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/no-nesting.md
    'promise/no-nesting': 'off',

    // Description: Disallow calling new on a Promise static method.
    // Description RU: Запрещает вызов new в статическом методе Promise.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/no-new-statics.md
    'promise/no-new-statics': 'off',

    // Description: Disallow using promises inside of callbacks.
    // Description RU: Запрещает использование обещаний внутри обратных вызовов.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/no-promise-in-callback.md
    'promise/no-promise-in-callback': 'off',

    // Description: Disallow return statements in finally().
    // Description RU: Запрещает операторы return в finally().
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/no-return-in-finally.md
    'promise/no-return-in-finally': 'off',

    // Description: Disallow wrapping values in Promise.resolve or Promise.reject when not needed.
    // Description RU: Запрещает перенос значений в Promise.resolve или Promise.reject, когда в этом нет необходимости.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/no-return-wrap.md
    'promise/no-return-wrap': 'off',

    // Description: Enforce consistent param names and ordering when creating new promises.
    // Description RU: Обеспечение единообразия имен параметров и упорядочения при создании новых обещаний.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/param-names.md
    'promise/param-names': 'off',

    // Description: Prefer async/await to the callback pattern.
    // Description RU: Шаблону обратного вызова предпочтительнее асинхронное ожидание.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/prefer-await-to-callbacks.md
    'promise/prefer-await-to-callbacks': 'off',

    // Description: Prefer await to then()/catch()/finally() for reading Promise values.
    // Description RU: Для чтения значений Promise предпочтительнее использовать await to then()/catch()/finally().
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/prefer-await-to-then.md
    'promise/prefer-await-to-then': 'off',

    // Description: Prefer catch to then(a, b)/then(null, b) for handling errors.
    // Description RU: Для обработки ошибок предпочтительнее использовать catch вместо then(a, b)/then(null, b).
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/prefer-catch.md
    'promise/prefer-catch': 'off',

    // Description: Disallow use of non-standard Promise static methods.
    // Description RU: Запрещает использование нестандартных статических методов Promise.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/spec-only.md
    'promise/spec-only': 'off',

    // Description: Enforces the proper number of arguments are passed to Promise functions.
    // Description RU: Обеспечивает передачу надлежащего количества аргументов в функции Promise.
    // URL:https://github.com/eslint-community/eslint-plugin-promise/blob/HEAD/docs/rules/valid-params.md
    'promise/valid-params': 'off',
  }
}
